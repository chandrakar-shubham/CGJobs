@extends('web.layout')
@section('title', ($item->hindi_title ?: $item->title).' — CGJobs GK')
@section('content')
<div class="container mx-auto px-4 py-10"><div class="max-w-3xl mx-auto"><div class="text-xs font-black uppercase tracking-widest text-slate-400">Static GK · {{ $item->category ?: 'General Knowledge' }}</div><h1 class="text-3xl md:text-5xl font-black mt-3 leading-tight">{{ $item->hindi_title ?: $item->title }}</h1><div class="bg-white border rounded-3xl p-6 md:p-8 mt-7 shadow-sm">@if($item->question)<div class="text-sm font-bold text-slate-400">Question</div><div class="text-xl font-extrabold mt-2">{{ $item->question }}</div>@endif @if($item->answer)<div class="mt-7"><div class="text-sm font-bold text-slate-400">Answer</div><div class="text-lg font-bold mt-2">{{ $item->answer }}</div></div>@endif @if($item->key_points)<div class="mt-7"><div class="text-sm font-bold text-slate-400 mb-2">Key Points</div><ul class="list-disc pl-6 space-y-2 text-slate-700">@foreach($item->key_points as $point)<li>{{ is_string($point) ? $point : json_encode($point,JSON_UNESCAPED_UNICODE) }}</li>@endforeach</ul></div>@endif @if($item->detailed_notes)<div class="mt-7 pt-7 border-t"><div class="text-sm font-bold text-slate-400">Detailed Notes</div><div class="whitespace-pre-line leading-7 mt-2 text-slate-700">{{ $item->detailed_notes }}</div></div>@endif @if($item->year_exam_reference)<div class="mt-7 rounded-xl bg-slate-50 p-4 text-sm"><strong>Exam reference:</strong> {{ $item->year_exam_reference }}</div>@endif<div class="mt-8"><a href="https://play.google.com/store" class="inline-flex bg-slate-950 text-white px-5 py-3 rounded-xl font-bold">📱 See in App</a></div></div>
@if($related->count())<h2 class="text-xl font-black mt-10 mb-4">Related GK</h2><div class="grid sm:grid-cols-2 gap-4">@foreach($related as $r)<a href="{{ route('gk.show',$r->custom_id ?: $r->id) }}" class="bg-white border rounded-2xl p-4 font-bold">{{ $r->hindi_title ?: $r->title }}</a>@endforeach</div>@endif</div></div>
@endsection