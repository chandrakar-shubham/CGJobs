@extends('web.layout')
@section('title', $lang === 'en' ? 'Notifications — CGJobs' : 'Notifications — CGJobs')
@section('content')
<div class="section"><div class="container" style="max-width:900px"><div class="section-head"><div><div class="section-title">{{ $lang==='en' ? 'Latest Notifications' : 'नवीनतम सूचनाएँ' }}</div><p style="color:#64748b">{{ $lang==='en' ? 'New recruitment and important updates published on CGJobs.' : 'CGJobs पर प्रकाशित नई भर्ती और महत्वपूर्ण अपडेट।' }}</p></div></div>
<div class="grid" style="gap:12px">@forelse($alerts as $alert)<a class="card" href="{{ $alert->action_url ?: ($alert->article_id ? route('job.show',$alert->article_id) : route('listing.jobs')) }}"><div style="display:flex;justify-content:space-between;gap:12px"><span class="pill" style="display:inline-block;padding:5px 9px;border-radius:999px;background:#eef2ff;font-size:11px;font-weight:800">{{ $alert->category }}</span><span style="font-size:11px;color:#94a3b8">{{ $alert->created_at?->diffForHumans() }}</span></div><h3>{{ $alert->title }}</h3><p>{{ $alert->short_description }}</p><span class="read">{{ $lang==='en' ? 'Read update →' : 'अपडेट पढ़ें →' }}</span></a>@empty<div class="card"><p>{{ $lang==='en' ? 'No notifications yet.' : 'अभी कोई नई सूचना नहीं है।' }}</p></div>@endforelse</div><div style="margin-top:20px">{{ $alerts->links() }}</div></div></div>
@endsection
