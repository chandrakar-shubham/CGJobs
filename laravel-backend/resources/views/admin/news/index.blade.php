@extends('layouts.admin')
@section('title','Current Affairs')
@section('content')
<div class="flex flex-wrap items-center justify-between gap-3 mb-5"><div><h2 class="text-2xl font-bold">Current Affairs</h2><p class="text-sm text-slate-500 mt-1">Manage exam-focused news independently from Jobs.</p></div><a href="{{route('admin.news.create')}}" class="px-4 py-2.5 rounded-xl bg-blue-700 text-white text-sm font-semibold hover:bg-blue-800"><i class="fa-solid fa-plus mr-2"></i>Create Current Affair</a></div>
<div class="grid grid-cols-2 md:grid-cols-4 gap-3 mb-5"><div class="bg-white rounded-2xl border p-4"><div class="text-xs text-slate-500">Total</div><div class="text-2xl font-bold">{{$news->total()}}</div></div><div class="bg-white rounded-2xl border p-4"><div class="text-xs text-slate-500">Published</div><div class="text-2xl font-bold text-emerald-600">{{\App\Models\News::where('status','published')->count()}}</div></div><div class="bg-white rounded-2xl border p-4"><div class="text-xs text-slate-500">Draft / Review</div><div class="text-2xl font-bold text-amber-600">{{\App\Models\News::whereIn('status',['draft','review'])->count()}}</div></div><div class="bg-white rounded-2xl border p-4"><div class="text-xs text-slate-500">Featured</div><div class="text-2xl font-bold text-purple-600">{{\App\Models\News::where('featured',true)->count()}}</div></div></div>
<form class="bg-white rounded-2xl border p-4 mb-5 grid md:grid-cols-4 gap-3"><input name="search" value="{{request('search')}}" placeholder="Search current affairs..." class="rounded-xl border-slate-200 border px-3 py-2 text-sm"><select name="category" class="rounded-xl border-slate-200 border px-3 py-2 text-sm"><option value="">All categories</option>@foreach($categories as $category)<option value="{{$category}}" @selected(request('category')===$category)>{{$category}}</option>@endforeach</select><select name="status" class="rounded-xl border-slate-200 border px-3 py-2 text-sm"><option value="">All statuses</option>@foreach(['draft','review','published','archived'] as $status)<option value="{{$status}}" @selected(request('status')===$status)>{{ucfirst($status)}}</option>@endforeach</select><button class="rounded-xl bg-slate-800 text-white px-4 py-2 text-sm font-semibold">Filter</button></form>
<div class="bg-white rounded-2xl border overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50 border-b"><tr><th class="text-left p-3">Article</th><th class="text-left p-3">Category</th><th class="text-left p-3">Relevance</th><th class="text-left p-3">Status</th><th class="text-right p-3">Actions</th></tr></thead><tbody class="divide-y">@forelse($news as $item)<tr><td class="p-3 min-w-[300px]"><div class="font-semibold">{{Str::limit($item->title,90)}}</div><div class="text-xs text-slate-500 mt-1">{{$item->source ?: 'Manual'}} · {{$item->published_at?->format('d M Y H:i')}}</div></td><td class="p-3">{{$item->category}}</td><td class="p-3">{{str_repeat('★',(int)$item->exam_relevance)}}<span class="text-slate-300">{{str_repeat('★',5-(int)$item->exam_relevance)}}</span></td><td class="p-3"><span class="px-2 py-1 rounded-lg text-xs font-semibold {{ $item->status==='published'?'bg-emerald-50 text-emerald-700':($item->status==='archived'?'bg-slate-100 text-slate-600':'bg-amber-50 text-amber-700') }}">{{ucfirst($item->status)}}</span></td><td class="p-3"><div class="flex justify-end gap-2">@if($item->status!=='published')<form method="POST" action="{{route('admin.news.publish',$item)}}">@csrf<button class="px-2.5 py-1.5 rounded-lg bg-emerald-600 text-white text-xs">Publish</button></form>@endif<a href="{{route('admin.news.edit',$item)}}" class="px-2.5 py-1.5 rounded-lg bg-blue-50 text-blue-700 text-xs">Edit</a><form method="POST" action="{{route('admin.news.destroy',$item)}}" onsubmit="return confirm('Delete this article?')">@csrf @method('DELETE')<button class="px-2.5 py-1.5 rounded-lg bg-rose-50 text-rose-700 text-xs">Delete</button></form></div></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-500">No current affairs articles yet. Create one or run the News Sync.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{$news->links()}}</div></div>
@endsection
